<UserControl
	x:Class="IGE.Views.InventoryFolderView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="{Binding DepthSpace}" />
			<ColumnDefinition
				Width="auto" />
			<ColumnDefinition
				Width="auto" />
			<ColumnDefinition
				Width="*" />
		</Grid.ColumnDefinitions>
		<Image
			Grid.Column="1"
			Stretch="None"
			Source="{Binding Image}" />
		<Label
			Grid.Column="2"
			Padding="5,0"
			Content="{Binding Text}" />
		<Label
			Grid.Column="3"
			Padding="5,0"
			Foreground="#FF808080"
			Content="{Binding ChildEntriesText}" />
	</Grid>
</UserControl>
