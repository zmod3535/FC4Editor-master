<UserControl
	x:Class="IGE.Views.ParamEnumButtonImageView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views"
	IsEnabled="{Binding Enabled}">
	<FrameworkElement.Resources>
		<ResourceDictionary>
			<BooleanToVisibilityConverter
				x:Key="BoolToVis" />
		</ResourceDictionary>
	</FrameworkElement.Resources>
	<ToggleButton
		Margin="2"
		ToolTip="{Binding DisplayName}"
		IsChecked="{Binding IsActive}"
		Visibility="{Binding EntryVisible, Converter={StaticResource BoolToVis}}">
		<Image
			Width="32"
			Height="32"
			Source="{Binding Image}" />
	</ToggleButton>
</UserControl>
