<UserControl
	x:Class="IGE.Views.ParamIntView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views"
	IsEnabled="{Binding Enabled}"
	Visibility="{Binding Visible}">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="Auto"
				MinWidth="100" />
			<ColumnDefinition
				Width="*" />
			<ColumnDefinition
				Width="50" />
		</Grid.ColumnDefinitions>
		<Label
			Grid.Column="0"
			HorizontalAlignment="Left"
			Content="{Binding DisplayName}" />
		<Slider
			Grid.Column="1"
			HorizontalAlignment="Stretch"
			TickFrequency="1"
			IsSnapToTickEnabled="True"
			Minimum="{Binding MinValue}"
			Maximum="{Binding MaxValue}"
			Value="{Binding Value}" />
		<TextBox
			Grid.Column="2"
			HorizontalAlignment="Stretch"
			Margin="2,2"
			Text="{Binding Value, StringFormat={}{0:D}}" />
	</Grid>
</UserControl>
