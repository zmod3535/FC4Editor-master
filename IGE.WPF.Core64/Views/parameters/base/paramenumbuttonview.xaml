<UserControl
	x:Class="IGE.Views.ParamEnumButtonView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views" xmlns:params="clr-namespace:IGE.Parameters"
	IsEnabled="{Binding Enabled}"
	Visibility="{Binding Visible}">
	<FrameworkElement.Resources>
		<ResourceDictionary>
			<DataTemplate
				DataType="{x:Type params:ParamEnumButtonText}"
				x:Key="{DataTemplateKey {x:Type params:ParamEnumButtonText}}">
				<views:ParamEnumButtonItemView />
			</DataTemplate>
			<DataTemplate
				DataType="{x:Type params:ParamEnumButtonImage}"
				x:Key="{DataTemplateKey {x:Type params:ParamEnumButtonImage}}">
				<views:ParamEnumButtonImageView />
			</DataTemplate>
			<DataTemplate
				DataType="{x:Type params:ParamEnumButtonImageText}"
				x:Key="{DataTemplateKey {x:Type params:ParamEnumButtonImageText}}">
				<views:ParamEnumButtonImageTextView />
			</DataTemplate>
		</ResourceDictionary>
	</FrameworkElement.Resources>
	<StackPanel>
		<Label
			Content="{Binding DisplayName}"
			Visibility="{Binding LabelVisibility}" />
		<ItemsControl
			ItemsSource="{Binding Values}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel
						Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
		</ItemsControl>
	</StackPanel>
</UserControl>
