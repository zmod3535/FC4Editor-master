<UserControl
	x:Class="IGE.Views.ParamTimeView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views"
	IsEnabled="{Binding Enabled}"
	Visibility="{Binding Visible}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition
				Width="75" />
		</Grid.ColumnDefinitions>
		<Label
			Grid.Row="0"
			Grid.ColumnSpan="2"
			Content="{Binding DisplayName}" />
		<Slider
			Grid.Row="1"
			Grid.Column="0"
			Minimum="0"
			Maximum="24"
			Value="{Binding SliderValue}" />
		<TextBlock
			Grid.Row="1"
			Grid.Column="1"
			Focusable="False"
			HorizontalAlignment="Center">
			<TextBlock.Text>
				<MultiBinding
					StringFormat="{}{0:D2}:{1:D2}:{2:D2}">
					<Binding
						Path="Value.Hours" />
					<Binding
						Path="Value.Minutes" />
					<Binding
						Path="Value.Seconds" />
				</MultiBinding>
			</TextBlock.Text>
		</TextBlock>
	</Grid>
</UserControl>
