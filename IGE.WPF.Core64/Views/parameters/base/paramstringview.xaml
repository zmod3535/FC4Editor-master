<UserControl
	x:Class="IGE.Views.ParamStringView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:IGE.Views"
	IsEnabled="{Binding Enabled}"
	Visibility="{Binding Visible}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Label
			Grid.Row="0"
			Content="{Binding DisplayName}" />
		<TextBox
			Grid.Row="1"
			Margin="5,0,5,5"
			Text="{Binding Value}" />
	</Grid>
</UserControl>
