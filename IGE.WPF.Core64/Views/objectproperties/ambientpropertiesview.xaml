<UserControl
	x:Class="IGE.Views.AmbientPropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:v="clr-namespace:IGE.ViewModels.Validation" xmlns:hlp="clr-namespace:IGE.Helpers" xmlns:views="clr-namespace:IGE.Views">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<TextBlock
			Grid.Row="0"
			Grid.ColumnSpan="2"
			Text="{hlp:Localize AMBIENT_SPAWN_OPTION}" />
		<ComboBox
			Grid.Row="1"
			Grid.ColumnSpan="2"
			ItemsSource="{Binding SpawnOptions}"
			SelectedItem="{Binding SpawnOption}">
			<ItemsControl.ItemTemplateSelector>
				<hlp:ComboBoxItemTemplateSelector>
					<hlp:ComboBoxItemTemplateSelector.SelectedTemplate>
						<DataTemplate>
							<TextBlock
								Text="{Binding Display}" />
						</DataTemplate>
					</hlp:ComboBoxItemTemplateSelector.SelectedTemplate>
					<hlp:ComboBoxItemTemplateSelector.DropDownTemplate>
						<DataTemplate>
							<TextBlock
								Text="{Binding Display}" />
						</DataTemplate>
					</hlp:ComboBoxItemTemplateSelector.DropDownTemplate>
				</hlp:ComboBoxItemTemplateSelector>
			</ItemsControl.ItemTemplateSelector>
		</ComboBox>
	</Grid>
</UserControl>
