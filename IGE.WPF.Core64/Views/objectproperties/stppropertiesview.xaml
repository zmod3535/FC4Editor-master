<UserControl
	x:Class="IGE.Views.STPPropertiesView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:v="clr-namespace:IGE.ViewModels.Validation" xmlns:hlp="clr-namespace:IGE.Helpers" xmlns:b="clr-namespace:IGE.ViewModels.Behaviours" xmlns:views="clr-namespace:IGE.Views">
	<Grid
		Margin="5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition
				Width="50" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="25" />
			<RowDefinition
				Height="25" />
			<RowDefinition
				Height="25" />
			<RowDefinition
				Height="25" />
			<RowDefinition
				Height="25" />
			<RowDefinition
				Height="25" />
		</Grid.RowDefinitions>
		<TextBlock
			Grid.Row="0"
			Grid.ColumnSpan="2"
			Text="{hlp:LocalizeCommon OBJ_PROPERTIES_DURATION}" />
		<Slider
			Grid.Row="1"
			Grid.Column="0"
			Minimum="1"
			Maximum="3600"
			Value="{Binding TargetedTime}" />
		<TextBox
			Grid.Row="1"
			Grid.Column="1"
			b:MaskBehaviour.Mask="^\d*$"
			b:InputBindingsManager.UpdatePropertySourceWhenEnterPressed="Text"
			Text="{Binding TargetedTimeText}" />
		<TextBlock
			Grid.Row="2"
			Grid.ColumnSpan="2"
			Text="{hlp:LocalizeCommon OBJ_PROPERTIES_COOLDOWN}" />
		<Slider
			Grid.Row="3"
			Grid.Column="0"
			Minimum="1"
			Maximum="3600"
			Value="{Binding CooldownTime}" />
		<TextBox
			Grid.Row="3"
			Grid.Column="1"
			b:MaskBehaviour.Mask="^\d*$"
			b:InputBindingsManager.UpdatePropertySourceWhenEnterPressed="Text"
			Text="{Binding CooldownTimeText}" />
		<TextBlock
			Grid.Row="4"
			Grid.ColumnSpan="2"
			Text="{hlp:LocalizeCommon OBJ_PROPERTIES_FACTION}" />
		<ComboBox
			Grid.Row="5"
			Grid.ColumnSpan="2"
			ItemsSource="{Binding UsedByAITypeOptions}"
			SelectedItem="{Binding UsedByAIType}"
			Visibility="{Binding AITypeVisibility}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<TextBlock
						Text="{Binding Display}" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ComboBox>
	</Grid>
</UserControl>
