<UserControl
	x:Class="IGE.Views.ToolView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:hlp="clr-namespace:IGE.Helpers" xmlns:views="clr-namespace:IGE.Views">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid
			Row="0">
			<DockPanel
				Visibility="{Binding HeaderVisible}">
				<Image
					Height="32"
					Source="{Binding ImageSource}" />
				<Label
					FontSize="20"
					Content="{Binding DisplayName}" />
			</DockPanel>
		</Grid>
		<Grid
			Row="1"
			Margin="2,2,2,2">
			<ScrollViewer
				Name="ScrollViewer"
				HorizontalScrollBarVisibility="Hidden"
				VerticalScrollBarVisibility="Auto">
				<FrameworkElement.Resources>
					<ResourceDictionary>
						<ResourceDictionary.MergedDictionaries>
							<ResourceDictionary
								Source="../ResourceDictionary/Params.xaml" />
						</ResourceDictionary.MergedDictionaries>
					</ResourceDictionary>
				</FrameworkElement.Resources>
				<ItemsControl
					ItemsSource="{Binding Parameters}"
					MaxWidth="{Binding ViewportWidth, ElementName=ScrollViewer}" />
			</ScrollViewer>
		</Grid>
	</Grid>
</UserControl>
